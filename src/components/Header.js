import React from 'react';

const Header = () => {
  return (
    <header style={{ padding: '1rem', background: '#ffcb05', color: '#2a75bb', textAlign: 'center' }}>
      <h1>Pokédex React</h1>
    </header>
  );
};

export default Header;
